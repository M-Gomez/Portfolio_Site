pipeline {
    agent {node {label 'portfolio-agent'}}

    stages {
        stage('Build') {
            steps {
                sh 'sudo apt install -f npm'
                sh 'npm install --force'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                sh 'npm start'
            }
        }
    }
}
