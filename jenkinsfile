pipeline {
    agent portfolio-agent

    stages {
        stage('Build') {
            steps {
                npm install --force

            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                npm start
            }
        }
    }
}
